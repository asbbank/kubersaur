apiVersion: v1
kind: ConfigMap
metadata:
  name: config-{{ template "home-ownership-explorer-ui.name" . }}
  namespace: {{ .Values.global.namespace }}
data:
  app.properties: |-
    app.custom.property=property-{{ .Values.global.cluster }}

